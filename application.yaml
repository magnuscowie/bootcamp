apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: php-mysql-app
  namespace: magnus
spec:
  destination:
    namespace: default
    server: https://kubernetes.default.svc
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

